# PDF Signer Tool

## Run locally

```bash
npm install
npm run dev
```

Open http://localhost:3000.

## MVP scope

- Upload PDF in browser (no server upload)
- Add text, checkbox, and generated signature overlays
- Drag overlays and edit selected overlay
- Multi-page PDF preview with thumbnails and zoom
- Export final PDF with overlays burned in using pdf-lib
